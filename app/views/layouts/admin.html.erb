<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="keywords" content="GMAT"/>
    <meta name="description" content=""/>
    <title>GMAT770内容管理系统</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag 'admin'%>
    <%= javascript_include_tag 'admin' %>
  </head>
  <body>
    <nav class="navbar navbar-default">
      <div class="container">
        <a class="navbar-brand" href="/admin">GMAT770内容管理系统</a>
        <% if current_user %>
          <p class="navbar-text navbar-right">当前用户 <a href="#" class="navbar-link"><%= current_user.name %></a></p>
        <% end %>
      </div>
    </nav>

    <div class="container admin-container">
      <div class="row">
        <div class="col col-md-2 nav-container">
          <%= render 'admin/admin_nav' %>
        </div>
        <div class="col col-md-10 content">
          <%= yield %>
        </div>
      </div>
    </div>

    <%= yield :scripts %>

  </body>
</html>
